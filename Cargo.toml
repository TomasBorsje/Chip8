[package]
name = "chip8"
version = "0.1.0"
edition = "2024"

# Comment the two lines below out to run locally
#[lib]
#crate-type = ["cdylib"]

[profile.dev]
overflow-checks = false

[dependencies]
wasm-bindgen = "0.2.120"
rand = "0.10.1"
getrandom = { version = "0.4.2", features = ["wasm_js"]}